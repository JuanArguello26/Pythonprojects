mylist = ["Juan","Pedro","Maria","Jhoana"] * 3 #we can repeat the list adding * and the times that we want to repeat
print(mylist)

#mylist2=[2,3,"Jhon"]
#We can concatenate 2 lists like this:
#mylist3=mylist+mylist2

#print(mylist3)


#mylist.append("Esteban") append adds the element at the end of the list

#mylist.insert(1,"Esteban") insert adds the element where i choose it

#mylist.extend(["Benito", "Carla", "Camilo"]) extends adds the last elements at the end of the list

#mylist.remove("Pedro") it deletes the chosen element

#mylist.pop() remove the last element of the list

#print(mylist.index("Pedro")) we use index when we need to know where's the number of element that i want to know // if we have 2 same elements, the index chooses the first one

#print("Juan" in mylist) evaluate with a boolean if the str called it's true or false

# imprimir el seleccionado en la lista print(mylist[1])

#imprimir de punto a a punto b print(mylist[0:3])


